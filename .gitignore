# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Superpowers brainstorm sessions
.superpowers/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Documentos de conteúdo (planilhas de treino do professor etc.) — nunca
# pertencem ao repositório de código; force-add se algum dia for necessário
*.docx

# Supabase CLI — estado de sessão local (project ref, pooler URL etc.)
supabase/.temp/

# Build cache do tsc
*.tsbuildinfo

# Cópias compiladas de configs TS — só o .ts é fonte de verdade
vite.config.js
vite.config.d.ts

# Patches soltos de debug — nunca devem ficar versionados
*.patch
