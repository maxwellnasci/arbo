--- supabase/functions/strava-sync/index.ts
+++ supabase/functions/strava-sync/index.ts
@@ -161,6 +161,7 @@
 
   const rawActivities = await activitiesRes.json() as StravaActivity[]
+  console.log('rawActivities length:', rawActivities.length)
   const runs = rawActivities.filter((a) => a.type === 'Run').slice(0, 10)
+  console.log('runs length:', runs.length)
 
   if (runs.length > 0) {
